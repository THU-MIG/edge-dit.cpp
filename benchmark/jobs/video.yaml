# text-to-video benchmark manifest: cross-system compare. Wan is 832x480, few frames.
# Per model: fp16/bf16 baseline (large via offload+max_vram), q8/q4_k resident, edge-only
# offload/auto tiers, then bare sentinels expected to OOM. Rows grouped by model.
# stable-diffusion.cpp omitted: cannot load Wan via a single --model dir (needs component-
# separated loading). Distilled wan21-t2v-1.3b-distill is transformer-only -> edge only.
name: video
task: text-to-video
prompts: 3
device: 0

edge-dit:
  model: [wan2-t2v-1.3b]                               # small: fp16 resident
  quant:
    - fp16
    - q8
    - q4_k
    - {model: wan21-t2v-1.3b-distill, type: fp16}
    - {model: wan21-t2v-1.3b-distill, type: q8}
    - {model: wan21-t2v-1.3b-distill, type: q4_k}
    - {model: wan2-t2v-14b,           type: fp16, offload: full, max_vram: 20}
    - {model: wan2-t2v-14b,           type: q4_k}
    - {model: wan2-t2v-14b,           offload: auto-fit, max_vram: 20}
    - {model: wan2-t2v-14b,           type: q8, offload: auto-allocate, max_vram: 20}
    - {model: wan2-t2v-14b,           type: fp16}                                # sentinel: expected OOM
    - {model: wan2-t2v-14b,           type: q8}                                  # sentinel: expected OOM
diffusers:                            # full-directory Wan only (transformer-only distill unsupported)
  model: [wan2-t2v-1.3b]
  quant:
    - bf16
    - fp8
    - {model: wan2-t2v-14b, type: bf16, offload: sequential}
    - {model: wan2-t2v-14b, type: fp8}
    - {model: wan2-t2v-14b, type: bf16}                                          # sentinel: expected OOM
