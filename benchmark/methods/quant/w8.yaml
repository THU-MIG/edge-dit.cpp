method_id: w8
kind: runtime
category: quant
description: >
  diffusers weight-only int8 via Optimum-Quanto (qint8 weights, activations left in
  fp16). The single diffusers 8-bit tier, used for ALL diffusers models (SD3 / SD3.5-turbo
  / flux / qwen). Mirrors edge/sd.cpp's q8_0 (also int8 weight-only) and tracks the bf16
  baseline.
options: {quant_weights: qint8, quant_activations: none}
cross_system: [diffusers]
convert_default: false
