schema_version: 1
model_id: flux-schnell
model_family: FLUX.1
model_name: FLUX.1-schnell
task: text-to-image
distilled: true                        # distilled: engine detect_distilled_default_steps auto-sets ~4 steps
model:
  public_id: black-forest-labs/FLUX.1-schnell
  local_path_ref: flux_schnell_dir     # add this ref in site
  format: diffusers-directory
prompt_set: ../prompts/text-to-image-v1.jsonl
generation:
  width: 1024
  height: 1024
  steps: 4                             # distilled low-step default
  guidance: 3.5
  cfg_scale: 1.0
  scheduler: model-default
