# edge-dit.cpp benchmark -- DIFFUSERS cross-system baseline dependencies.
#
# Install this ONLY when you want to (re)generate the HuggingFace diffusers
# system-under-test (engine/runners/diffusers.py, scripts/run_diffusers_e2e.py)
# for cross-system comparison. If you only run edge-dit.cpp and score existing
# outputs, requirements/core.txt is enough.
#
# Layered on top of core: install core first (it carries torch/transformers).
#     pip install -r benchmark/requirements/core.txt
#     pip install -r benchmark/requirements/diffusers.txt
-r core.txt

diffusers>=0.39.0          # FluxPipeline / FluxKontextPipeline / QwenImageEditPipeline / WanPipeline
accelerate>=1.0            # accelerate model CPU offload path
optimum-quanto>=0.2.7      # diffusers w8 quantization: qint8 weight-only (optimum.quanto)
safetensors>=0.4           # quantized weight (de)serialization
